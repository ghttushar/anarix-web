## Signals height correction

- Change only the Signals page (`/alerts` and `/alerts/stack`).
- Increase both the center signal list and right review workspace from `calc(100vh - 190px)` to `calc(100vh - 140px)`.
- This adds 50px of usable height and leaves only the intended small bottom inset, aligning the workspace bottom near the sidebar profile level.
- Keep all other spacing, pages, content, and behavior untouched.
- Verify the result at the current 1377×965 viewport.